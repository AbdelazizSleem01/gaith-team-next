# دليل نظام Light/Dark Mode

## نظرة عامة
تم تطبيق نظام عصري وجميل للتبديل بين الوضع الفاتح والمظلم في الموقع.

## المميزات

### 🎨 التصميم
- **Dark Mode (الوضع المظلم)**: خلفيات داكنة مع تدرجات لونية جميلة
- **Light Mode (الوضع الفاتح)**: خلفيات فاتحة نظيفة وعصرية
- **انتقالات سلسة**: جميع التحولات بين الأوضاع سلسة وجميلة
- **تأثيرات متحركة**: خلفيات متحركة وتأثيرات بصرية جذابة

### 🔧 التقنيات المستخدمة
- **React Context API**: لإدارة حالة الثيم عبر التطبيق
- **Tailwind CSS**: مع دعم Dark Mode
- **Local Storage**: لحفظ تفضيلات المستخدم
- **CSS Variables**: لألوان ديناميكية

### 📁 الملفات المحدثة

#### 1. `components/ThemeProvider.tsx` (جديد)
- مزود السياق لإدارة الثيم
- حفظ واستعادة التفضيلات
- دعم تفضيلات النظام

#### 2. `tailwind.config.ts`
- إضافة `darkMode: 'class'`
- ألوان مخصصة للثيم
- رسوم متحركة جديدة

#### 3. `app/globals.css`
- متغيرات CSS للألوان
- أنماط Dark/Light Mode
- تأثيرات متحركة للخلفية
- أدوات مساعدة للكروت والأزرار

#### 4. `components/Navbar.tsx`
- زر تبديل الثيم محسّن
- تأثيرات بصرية عند التبديل
- دعم كامل للثيم

#### 5. `components/Header.tsx`
- تحديث الألوان حسب الثيم
- تأثيرات متحركة محسّنة

#### 6. `components/Footer.tsx`
- تكيف مع الثيم الحالي
- ألوان ديناميكية

#### 7. `app/page.tsx`
- جميع المكونات تدعم الثيم
- كروت واختبارات متجاوبة مع الثيم

## 🎯 كيفية الاستخدام

### للمستخدمين
1. اضغط على أيقونة الشمس/القمر في شريط التنقل
2. سيتم حفظ اختيارك تلقائياً
3. عند العودة للموقع، سيتم استعادة اختيارك

### للمطورين

#### استخدام ThemeProvider
```tsx
import { useTheme } from '@/components/ThemeProvider';

function MyComponent() {
  const { theme, toggleTheme, setTheme } = useTheme();
  
  return (
    <div className={theme === "dark" ? "text-white" : "text-black"}>
      <button onClick={toggleTheme}>تبديل الثيم</button>
    </div>
  );
}
```

#### استخدام Tailwind Classes
```tsx
// للخلفيات
<div className={theme === "dark" ? "bg-gray-900" : "bg-white"}>

// للنصوص
<p className={theme === "dark" ? "text-white" : "text-gray-800"}>

// للحدود
<div className={theme === "dark" ? "border-white/10" : "border-gray-200"}>
```

## 🎨 الألوان المستخدمة

### Dark Mode
- **Primary Background**: `#0a0e1a`
- **Secondary Background**: `#0f1729`
- **Text Primary**: `#f8fafc`
- **Text Secondary**: `#cbd5e1`

### Light Mode
- **Primary Background**: `#ffffff`
- **Secondary Background**: `#f8fafc`
- **Text Primary**: `#0f172a`
- **Text Secondary**: `#334155`

### Brand Colors (ثابتة)
- **Primary**: `#4361ee`
- **Secondary**: `#7209b7`
- **Accent**: `#f72585`

## 🚀 التحسينات المستقبلية
- [ ] إضافة ثيمات إضافية (أزرق، أخضر، إلخ)
- [ ] وضع التباين العالي
- [ ] تخصيص الألوان من قبل المستخدم
- [ ] مزامنة التفضيلات عبر الأجهزة

## 📝 ملاحظات
- النظام يدعم تفضيلات النظام تلقائياً
- جميع التحولات سلسة ومحسّنة للأداء
- التصميم متجاوب بالكامل مع جميع الأحجام
